apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${APP_NAME}-postgres-data
  namespace: ${K8S_NAMESPACE}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
